import { StyleSheet } from "react-native";


const appStyles = StyleSheet.create({
    container: {
        flex: 1
    }
})

const mainStyles = StyleSheet.create({
    container: {
        flex: 1,
        paddingHorizontal: 10,
    }
})

export { appStyles, mainStyles }